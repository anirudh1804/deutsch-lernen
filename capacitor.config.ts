import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.germanlearn.app',
  appName: 'German Learning App',
  webDir: 'dist',
  androidScheme: 'https'
};

export default config;
